services:
  amiabot-pages:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: amiabot-pages
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
      SEKAI_ASSET: "haruki,snowy,uni"
      PJSK_PROFILE_BASEURL: ""
      PJSK_PROFILE_HEADERS: '{}'
    restart: unless-stopped
